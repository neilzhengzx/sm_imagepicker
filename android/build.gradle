apply plugin: 'com.android.library'

def safeExtGet(prop, fallback) {
    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}

android {
    compileSdkVersion safeExtGet('compileSdkVersion', 26)
    buildToolsVersion safeExtGet('buildToolsVersion', '26.0.2')

    defaultConfig {
        minSdkVersion safeExtGet('minSdkVersion', 16)
        targetSdkVersion safeExtGet('targetSdkVersion', 26)
        versionCode 1
        versionName "1.0"
    }
    lintOptions {
        abortOnError false
    }

    buildTypes {
        release {
            consumerProguardFiles 'proguard-rules.pro'
        }
    }
}

repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
    maven {
            url 'https://maven.google.com/'
            name 'Google'
        }
}

dependencies {
    compile 'com.facebook.react:react-native:+'
    compile 'com.github.lovetuzitong:MultiImageSelector:1.2'
    compile files('libs/pldroid-shortvideo-1.10.0.jar')
    compile "com.android.support:appcompat-v7:${safeExtGet('supportLibraryVersion', '26.1.0')}"
    compile "com.facebook.fresco:fresco:$rootProject.ext.gifVersion"
    compile "com.android.support:recyclerview-v7:${safeExtGet('supportLibraryVersion', '26.1.0')}"
}
  